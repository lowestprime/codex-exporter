interface:
  display_name: "Codex Thread Export"
  short_description: "Export local Codex sessions with maps, manifests, and robust filtering."
  default_prompt: "Export the requested local Codex session. Ask for a session ID only when none is available."
policy:
  allow_implicit_invocation: false
